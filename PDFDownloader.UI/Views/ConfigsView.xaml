<UserControl x:Class="PDFDownloader.UI.Views.ConfigsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border Style="{StaticResource SectionBorderStyle}">

        <Grid>
            <!-- Configs Layout -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="8*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <!-- Excel TextBlock -->
            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="Excel File:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>
            
            <!-- Excel TextBox -->
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Grid.ColumnSpan="3"
                     Text="{Binding ExcelFilePath}"
                     FontSize="12"
                     Foreground="{StaticResource Brush.TextPrimary}"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     IsReadOnly="True"
                     Focusable="False"
                     Cursor="Arrow">
            </TextBox>
            
            <!-- Excel Browse Button -->
            <Button Grid.Row="0"
                    Grid.Column="4"
                    Content="Browse"
                    Background="{StaticResource Brush.Accent}"
                    Foreground="{StaticResource Brush.Surface}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding BrowseExcelCommand}">
            </Button>

            <!-- Output TextBlock -->
            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Output Folder:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- Output TextBox -->
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Grid.ColumnSpan="3"
                     Text="{Binding OutputFolderPath}"
                     FontSize="12"
                     Foreground="{StaticResource Brush.TextPrimary}"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     IsReadOnly="True"
                     Focusable="False"
                     Cursor="Arrow">
            </TextBox>

            <!-- Output Browse Button -->
            <Button Grid.Row="1"
                    Grid.Column="4"
                    Content="Browse"
                    Background="{StaticResource Brush.Accent}"
                    Foreground="{StaticResource Brush.Surface}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding BrowseFolderCommand}">
            </Button>
            
            <!-- Starting Row TextBlock -->
            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Starting Row:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>
            
            <!-- Starting Row TextBox -->
            <TextBox Grid.Row="2"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter the starting row of the data in the Excel file"
                     Text="{Binding StartingRowStr, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>
            
            <!-- BR Nummer TextBlock -->
            <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="BR Nummer:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- BR Nummer TextBox -->
            <TextBox Grid.Row="3"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter column for the BR Nummer"
                     Text="{Binding BrNummer, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>

            <!-- Primary Url TextBlock -->
            <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Text="Primary URL:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- Primary Url TextBox -->
            <TextBox Grid.Row="4"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter column for the primary URL"
                     Text="{Binding PrimaryUrl, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>

            <!-- Primary Url TextBlock -->
            <TextBlock Grid.Row="5"
                       Grid.Column="0"
                       Text="Secondary URL:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- Primary Url TextBox -->
            <TextBox Grid.Row="5"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter column for the secondary URL"
                     Text="{Binding SecondaryUrl, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>
            
            <!-- Max Concurrency TextBlock -->
            <TextBlock Grid.Row="2"
                       Grid.Column="3"
                       Text="{Binding MaxConcurrency, StringFormat=Max Concurrency: {0}}"
                       FontSize="12"
                       FontWeight="Bold"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center">
            </TextBlock>
            
            <!-- Max Concurrency Slider -->
            <Slider Grid.Row="3"
                    Grid.Column="3"
                    Minimum="1"
                    Maximum="30"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    VerticalAlignment="Center"
                    Value="{Binding MaxConcurrency, UpdateSourceTrigger=PropertyChanged}">
            </Slider>

            <!-- Download Button -->
            <Button Grid.Row="5"
                    Grid.Column="4"
                    Content="Download"
                    Background="{StaticResource Brush.Accent}"
                    Foreground="{StaticResource Brush.Surface}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding StartDownloadCommand}">
            </Button>

        </Grid>

    </Border>
</UserControl>
