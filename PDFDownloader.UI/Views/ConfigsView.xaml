<UserControl x:Class="PDFDownloader.UI.Views.ConfigsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border Style="{StaticResource SectionBorderStyle}">

        <Grid>
            <!-- Configs Layout -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="10*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <!-- Excel TextBlock -->
            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="Excel File:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>
            
            <!-- Excel TextBox -->
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Grid.ColumnSpan="3"
                     Text="{Binding ExcelFilePath}"
                     FontSize="12"
                     Foreground="{StaticResource Brush.TextPrimary}"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     IsReadOnly="True"
                     Focusable="False"
                     Cursor="Arrow">
            </TextBox>
            
            <!-- Excel Browse Button -->
            <Button Grid.Row="0"
                    Grid.Column="4"
                    Content="Browse"
                    Background="{StaticResource Brush.Accent}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding BrowseExcelCommand}">
            </Button>

            <!-- Output TextBlock -->
            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Output Folder:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- Output TextBox -->
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Grid.ColumnSpan="3"
                     Text="{Binding OutputFolderPath}"
                     FontSize="12"
                     Foreground="{StaticResource Brush.TextPrimary}"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     IsReadOnly="True"
                     Focusable="False"
                     Cursor="Arrow">
            </TextBox>

            <!-- Output Browse Button -->
            <Button Grid.Row="1"
                    Grid.Column="4"
                    Content="Browse"
                    Background="{StaticResource Brush.Accent}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding BrowseFolderCommand}">
            </Button>
            
            <!-- Starting Row TextBlock -->
            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Starting Row:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>
            
            <!-- Starting Row TextBox -->
            <TextBox Grid.Row="2"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter the starting row of the data in the Excel file"
                     Text="{Binding StartingRowStr, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>
            
            <!-- BR Nummer TextBlock -->
            <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="BR Nummer:"
                       FontSize="12"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left">
            </TextBlock>

            <!-- Starting Row TextBox -->
            <TextBox Grid.Row="3"
                     Grid.Column="1"
                     FontSize="12"
                     BorderThickness="1"
                     BorderBrush="{StaticResource Brush.TextPrimary}"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     ToolTip="Enter column for the BR Nummer"
                     Text="{Binding BrNummer, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>

            <!-- Download Button -->
            <Button Grid.Row="5"
                    Grid.Column="4"
                    Content="Download"
                    Background="{StaticResource Brush.Accent}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Command="{Binding StartDownloadCommand}">
            </Button>

        </Grid>

    </Border>
</UserControl>
