<Window x:Class="PDFDownloader.UI.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PDFDownloader.UI"
        xmlns:vm="clr-namespace:PDFDownloader.UI.ViewModels"
        xmlns:views="clr-namespace:PDFDownloader.UI.Views"
        mc:Ignorable="d"
        Title="PDF Downloader" Height="650" Width="820"
        Background="{StaticResource Brush.Background}">
    
    <Grid>
        
        <!-- Application Layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Header View -->
        <views:HeaderView Grid.Row="0"
                          DataContext="{Binding HeaderViewModel}">
        </views:HeaderView>
        
        <!-- Configs View -->
        <views:ConfigsView Grid.Row="1"
                           DataContext="{Binding ConfigsViewModel}">
        </views:ConfigsView>
        
    </Grid>
    
</Window>
